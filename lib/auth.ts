/**
 * =============================================================================
 * 1HEALTH AUTHENTICATION TYPES
 * =============================================================================
 *
 * This file contains ONLY types and interfaces that can be safely imported
 * in both client and server contexts.
 *
 * For server-side auth utilities (authFetch, getOneHealthBaseUrl, etc.),
 * import from "@/lib/auth-server" instead.
 *
 * =============================================================================
 */

export type { ApiDebugInfo } from "./api-debug"
